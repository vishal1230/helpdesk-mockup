{
  "name": "helpdesk-backend",
  "version": "1.0.0",
  "description": "Helpdesk API using json-server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --watch db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": "18.x"
  }
}
